# DEMO ONLY. A real cluster must not commit secret material to git - use
# sealed-secrets, SOPS, or an external-secrets operator. stringData here keeps the
# demo self-contained and is plainly not a real credential.
apiVersion: v1
kind: Secret
metadata:
  name: k3s-demo-secret
type: Opaque
stringData:
  API_TOKEN: "demo-token-not-a-real-secret"
